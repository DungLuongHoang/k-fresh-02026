# Helper Scripts

This directory can hold optional helper scripts used by the `generate-testcase` skill. 
For instance:
- A script to scrape requirements from Jira.
- A script to generate test case templates based on a specific framework schema.
